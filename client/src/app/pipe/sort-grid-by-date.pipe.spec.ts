import { SortGridByDatePipe } from './sort-grid-by-date.pipe';

describe('SortGridByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortGridByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
